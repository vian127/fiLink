export const styleOptions = {
  // 边线颜色。
  strokeColor: 'red',
  // 填充颜色。当参数为空时，圆形将没有填充效果。
  fillColor: 'red',
  // 边线的宽度，以像素为单位。
  strokeWeight: 3,
  // 边线透明度，取值范围0 - 1。
  strokeOpacity: 0.8,
  // 填充的透明度，取值范围0 - 1。
  fillOpacity: 0.6,
  // 边线的样式，solid或dashed。
  strokeStyle: 'solid'
};
// 图标大小
export const ICON_SIZE = '18-24';
// 区域点大小
export const POINT_SIZE = '50-50';
